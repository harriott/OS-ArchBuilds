# vim: set ft=sh tw=0:
# ~/.bashrc
# Joseph Harriott, Sun 12 May 2019
# source /root/.bashrc   if you've changed something here...

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# suggested by Arch:
alias ls='ls --color=auto'
# PS1='[\u@\h \W]\$ '

# ---------------
# my preferences:
# ---------------
set -o vi

alias p='ping -c 3 8.8.8.8'
alias s="stat -c '%A %a %h %U %G %s %y %n'"

# History
alias h='history'
export HISTCONTROL=ignorespace:erasedups
export HISTIGNORE='?:??'
PROMPT_COMMAND='history -a'
shopt -s histappend

# a colorised (bold red) prompt:
PS1='\[\e[1;31m\][\u@\h \W]\$\[\e[0m\] '

# find system stuff:
# ------------------
fs () {
	find / -path /home/jo -prune -o -path /mnt -prune -o -path /run/media -prune -o -iname *$1* -print
}

