# vim: fdl=1 fdm=expr ft=sh.shfold:
#
# ~/.bash_profile
# get my  .bashrc  settings working in  Console
#
# for some reason changes here are only implemented after a reboot

#=> 0 $ARCHBUILDS
export ARCHBUILDS=/mnt/SDSSDA240G/Dropbox/JH/core/IT_stack/onGitHub/ArchBuilds

#=> 1 move on to generic bash_profile
[[ -f $ARCHBUILDS/root/bash_profile ]] && . $ARCHBUILDS/root/bash_profile

