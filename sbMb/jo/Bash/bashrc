#!/bin/bash
# vim: fdl=1 ft=sh sw=2:

# $machBld/jo/Bash/bashrc  symlinked in  $bSc/symlinks/jo-0.sh  e -la ~/.bashrc

[[ $- != *i* ]] && return  # if not running interactively, don't do anything

export CHROOT=/mnt/SD480GSSDPlus/chroot
export VsLocation=/mnt/ST4000VN008  # principal location
  export VsLmore=(/mnt/SDU3D1TB /mnt/WD1001FALS)
export VsLocations=(/mnt/ST4000VN008 /mnt/SDU3D1TB /mnt/WD1001FALS)
#  o ${VsLocations[@]}  # path to my video stores

#=> 0 load the rest of my bashrc
source $Bash/bashrc

#=> 1 OBS mkv grabs convert
ignore () { stat -c '%Y  %s  %n' "$1" >> $ulL/OBSmkv/ignore; }
# ignore <an_mkv_to_ignore>
# for m in *.mkv; do ignore "$m"; done

omc() { bash $ulL/OBSmkv/shrink.sh; fin; } # used in  $OSAB/sbMb/jo/autostart.sh

#=> 2 Created by `pipx` on 2022-08-15 19:39:50
export PATH="$PATH:/home/jo/.local/bin"

